<!-- Comments Container -->
<div class="comments">
  <!-- Loop through each comment in the context -->
  {{#each this}}
    <!-- Individual Comment Section -->
    <div>
      <!-- Comment Content with User and Date Information -->
      <p class="card-body2">
        {{commentContent}} - {{user.username}} {{format_date dateCreated}}
      </p>
    </div>
  {{/each}}
</div>